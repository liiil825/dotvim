Installation:

  git clone git://github.com/liiil825/dotvim.git ~/.vim
  git clone http://github.com/gmarik/vundle.git ~/.vim/bundle/Vundle.vim


Create symlinks:

  ln -s ~/.vim/.vimrc ~/.vimrc

  vim .vimrc
  :PluginInstall

